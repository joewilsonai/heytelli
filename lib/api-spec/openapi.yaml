openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: matches
    description: Bumble CRM matches
  - name: storage
    description: Object storage upload and serving endpoints
  - name: chat
    description: HeyTelli private chat operations
  - name: feedback
    description: Product feedback operations
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /matches:
    get:
      operationId: listMatches
      tags: [matches]
      summary: List all matches
      responses:
        "200":
          description: Matches
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Match"
    post:
      operationId: createMatch
      tags: [matches]
      summary: Create a match from an initial screenshot
      description: |
        Creates a new match record from an uploaded screenshot. The server
        runs vision extraction on the screenshot to suggest a name and
        initial profile. The screenshot is appended to the match's conversation log.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MatchCreateInput"
      responses:
        "201":
          description: Match created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchDetail"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/preview:
    post:
      operationId: previewMatchExtraction
      tags: [matches]
      summary: Run vision extraction on an uploaded screenshot without persisting a match
      description: |
        Given the object path of an already-uploaded screenshot, runs the AI
        vision model and returns the suggested name, vibe tags, and extracted
        profile so the user can review and edit before the match is created.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScreenshotInput"
      responses:
        "200":
          description: Extraction preview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExtractionPreview"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/{id}:
    get:
      operationId: getMatch
      tags: [matches]
      summary: Get a match with its screenshots
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Match
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
    patch:
      operationId: updateMatch
      tags: [matches]
      summary: Update a match's editable fields
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MatchUpdate"
      responses:
        "200":
          description: Updated match
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
    delete:
      operationId: deleteMatch
      tags: [matches]
      summary: Delete a match
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/{id}/screenshots:
    get:
      operationId: listScreenshots
      tags: [matches]
      summary: List all screenshots for a match in chronological order
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Screenshots
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Screenshot"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
    post:
      operationId: addScreenshot
      tags: [matches]
      summary: Add a screenshot to a match and update the extracted profile
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScreenshotInput"
      responses:
        "200":
          description: Updated match
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/{id}/rescore:
    post:
      operationId: rescoreMatch
      tags: [matches]
      summary: Re-run AI extraction on the latest screenshot to refresh scores
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Match with refreshed extraction
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchDetail"
        "400":
          description: No screenshots to score from
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/{id}/date-brief:
    post:
      operationId: generateDateBrief
      tags: [matches]
      summary: Generate a pre-date prep brief using full match context
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Date brief markdown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DateBriefResult"
        "400":
          description: No upcoming date scheduled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/{id}/red-flags:
    get:
      operationId: getRedFlagRadar
      tags: [matches]
      summary: Analyze conversation and date history for behavioral red/green flags
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Red flag radar result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedFlagRadarResult"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/{id}/cheat-sheet:
    get:
      operationId: getCheatSheet
      tags: [matches]
      summary: Get 3 quick-reply suggestions (playful / curious / direct) for current chat state
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Cheat sheet replies
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheatSheetResult"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/{id}/tag-suggestions:
    get:
      operationId: getTagSuggestions
      tags: [matches]
      summary: AI-proposed tag adds/removes based on latest match context
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Tag suggestions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagSuggestionResult"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/{id}/tag-history:
    get:
      operationId: getTagHistory
      tags: [matches]
      summary: Chronological log of every tag add/remove on this match
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Tag history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagHistoryResult"
  /matches/{id}/tags/apply:
    post:
      operationId: applyTagSuggestions
      tags: [matches]
      summary: Apply AI tag suggestions, persisting the diff as events
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagSuggestionApplyInput"
      responses:
        "200":
          description: Updated match
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/{id}/response-stats:
    get:
      operationId: getResponseStats
      tags: [matches]
      summary: Compute her vs. user reply cadence from screenshot history
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Response cadence stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResponseStats"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/weekly-debrief:
    get:
      operationId: getWeeklyDebrief
      tags: [matches]
      summary: Cross-match weekly Sunday debrief for the entire active pipeline
      responses:
        "200":
          description: Weekly debrief
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeeklyDebriefResult"
  /matches/auto-archive-candidates:
    get:
      operationId: getAutoArchiveCandidates
      tags: [matches]
      summary: List active matches that have gone fully cold and are candidates to archive/ghost
      responses:
        "200":
          description: Auto-archive candidates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AutoArchiveCandidate"
  /analytics/funnel:
    get:
      operationId: getFunnelStats
      tags: [matches]
      summary: Conversion funnel and pipeline analytics
      responses:
        "200":
          description: Funnel stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FunnelStats"
  /matches/stale-nudges:
    get:
      operationId: getStaleNudges
      tags: [matches]
      summary: List matches that have gone quiet with AI-generated re-engagement openers
      description: |
        Returns active matches where the last speaker was her or unknown AND
        her last reply was more than 36 hours ago. For each, includes 3
        suggested openers tailored to the conversation.
      responses:
        "200":
          description: List of stale matches with openers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StaleNudge"
  /matches/{id}/voice-note-feedback:
    post:
      operationId: voiceNoteFeedback
      tags: [matches]
      summary: Critique a voice note the user is about to send to a match
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VoiceDebriefInput"
      responses:
        "200":
          description: Voice note feedback
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoiceNoteFeedbackResult"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/{id}/in-person-recording:
    post:
      operationId: inPersonRecording
      tags: [matches]
      summary: Transcribe and analyze an in-person date recording (requires consent)
      description: |
        Transcribes an audio recording of an in-person interaction and runs
        the same debrief analysis. Requires `bothPartiesConsented=true` —
        the client is responsible for collecting that consent on-camera.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InPersonRecordingInput"
      responses:
        "200":
          description: Recording analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoiceDebriefResult"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /matches/{id}/voice-debrief:
    post:
      operationId: voiceDebrief
      tags: [matches]
      summary: Transcribe a voice debrief audio recording and analyze it
      description: |
        Accepts an already-uploaded audio file (object path), transcribes it
        via Whisper, then asks the LLM to extract structured insights:
        summary, vibe, green/red flags, suggested next move, and revised
        scores. Server persists the transcript appended to match notes and
        applies revised scores.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VoiceDebriefInput"
      responses:
        "200":
          description: Debrief analysis with the updated match detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoiceDebriefResult"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /chat/conversations:
    get:
      operationId: listChatConversations
      tags: [chat]
      summary: List all chat conversations
      responses:
        "200":
          description: List of conversations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatConversation"
    post:
      operationId: createChatConversation
      tags: [chat]
      summary: Create a new chat conversation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatConversationInput"
      responses:
        "201":
          description: Created conversation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatConversation"
  /chat/conversations/{id}:
    get:
      operationId: getChatConversation
      tags: [chat]
      summary: Get conversation with messages
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Conversation with messages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatConversationWithMessages"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
    delete:
      operationId: deleteChatConversation
      tags: [chat]
      summary: Delete a conversation
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /chat/conversations/{id}/messages:
    get:
      operationId: listChatMessages
      tags: [chat]
      summary: List messages in a conversation
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: List of messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatMessage"
    post:
      operationId: sendChatMessage
      tags: [chat]
      summary: Send a message and stream the AI response (SSE)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatMessageInput"
      responses:
        "200":
          description: SSE stream of assistant response chunks
          content:
            text/event-stream: {}
  /storage/uploads/request-url:
    post:
      tags: [storage]
      operationId: requestUploadUrl
      summary: Request a presigned URL for file upload
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UploadUrlRequest"
      responses:
        "200":
          description: Presigned upload URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadUrlResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /feedback:
    post:
      operationId: createProductFeedback
      tags: [feedback]
      summary: Capture lightweight beta feedback from product moments
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductFeedbackInput"
      responses:
        "201":
          description: Feedback saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductFeedback"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status: { type: string }
      required: [status]
    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error: { type: string }
    ProductFeedbackInput:
      type: object
      required: [event, answer]
      properties:
        event: { type: string, minLength: 1, maxLength: 120 }
        answer: { type: string, minLength: 1, maxLength: 120 }
        matchId: { type: ["integer", "null"] }
        context:
          type: object
          additionalProperties: { type: string, maxLength: 240 }
          description: Allowlisted context only; no screenshots, phone numbers, transcripts, or notes.
    ProductFeedback:
      type: object
      required: [id, matchId, event, answer, context, createdAt]
      properties:
        id: { type: integer }
        matchId: { type: ["integer", "null"] }
        event: { type: string }
        answer: { type: string }
        context:
          type: object
          additionalProperties: { type: string }
        createdAt: { type: string, format: date-time }
    MatchScore:
      type: object
      required: [value, rationale]
      properties:
        value:
          oneOf:
            - type: integer
              minimum: 0
              maximum: 10
            - type: "null"
        rationale: { type: ["string", "null"] }
    MatchScores:
      type: object
      required: [sexPotential, conversionAbility, chemistry]
      properties:
        sexPotential:
          $ref: "#/components/schemas/MatchScore"
        conversionAbility:
          $ref: "#/components/schemas/MatchScore"
        chemistry:
          $ref: "#/components/schemas/MatchScore"
    ExtractedProfile:
      type: object
      required:
        [job, location, interests, mentionedTopics, conversationTone, scores]
      properties:
        job: { type: ["string", "null"] }
        location: { type: ["string", "null"] }
        interests:
          type: array
          items: { type: string }
        mentionedTopics:
          type: array
          items: { type: string }
        conversationTone: { type: ["string", "null"] }
        scores:
          $ref: "#/components/schemas/MatchScores"
    ScoreHistoryPoint:
      type: object
      required: [sexPotential, conversionAbility, chemistry, createdAt]
      properties:
        sexPotential: { type: ["integer", "null"], minimum: 0, maximum: 10 }
        conversionAbility:
          { type: ["integer", "null"], minimum: 0, maximum: 10 }
        chemistry: { type: ["integer", "null"], minimum: 0, maximum: 10 }
        createdAt: { type: string, format: date-time }
    DateHistoryEntry:
      type: object
      required: [id, when, location, recap, createdAt]
      properties:
        id: { type: string }
        when: { type: string, format: date-time }
        location: { type: string }
        recap: { type: string }
        createdAt: { type: string, format: date-time }
    MatchReadSnapshot:
      type: object
      required: [body, generatedAt, screenshotCountAt]
      properties:
        body:
          type: string
          description: Persisted latest AI read for this connection.
        generatedAt:
          type: string
          format: date-time
        screenshotCountAt:
          type: integer
          description: Successfully analyzed screenshot count at generation time.
    MatchReadFreshness:
      type: string
      enum: [current, stale, missing]
      description: |
        current = the persisted read reflects all analyzed screenshots.
        stale   = newer screenshots exist or some screenshots need analysis.
        missing = no read has ever been generated.
    DateSafetyPlan:
      type: object
      required:
        [
          trustedCircleName,
          transportPlan,
          checkInAt,
          expectedEndAt,
          codeWord,
          circleNote,
          shareLiveLocation,
          safeDateChecklist,
          circleCheckStatus,
          lastCircleCheckAt,
          coverModeEnabled,
          coverModeTheme,
          dateModeStatus,
          dateModeStartedAt,
          dateModeClosedAt,
          updatedAt,
        ]
      properties:
        trustedCircleName:
          type: ["string", "null"]
          description: First name or label of the trusted person/circle. Phone numbers are not stored.
        transportPlan:
          type: ["string", "null"]
          description: User-entered transport or exit plan.
        checkInAt:
          type: ["string", "null"]
          format: date-time
        expectedEndAt:
          type: ["string", "null"]
          format: date-time
        codeWord:
          type: ["string", "null"]
          description: Optional word the user can share with trusted contacts.
        circleNote:
          type: ["string", "null"]
          description: Optional user-authored note shown in the Date Card preview.
        shareLiveLocation:
          type: boolean
          description: User intent only; the app does not continuously track by default.
        safeDateChecklist:
          $ref: "#/components/schemas/SafeDateChecklist"
        circleCheckStatus:
          oneOf:
            - $ref: "#/components/schemas/CircleCheckStatus"
            - type: "null"
        lastCircleCheckAt:
          type: ["string", "null"]
          format: date-time
        coverModeEnabled:
          type: boolean
        coverModeTheme:
          oneOf:
            - $ref: "#/components/schemas/CoverModeTheme"
            - type: "null"
        dateModeStatus:
          oneOf:
            - $ref: "#/components/schemas/DateModeStatus"
            - type: "null"
        dateModeStartedAt:
          type: ["string", "null"]
          format: date-time
        dateModeClosedAt:
          type: ["string", "null"]
          format: date-time
        updatedAt:
          type: string
          format: date-time
    DateSafetyPlanInput:
      type: object
      required:
        [
          trustedCircleName,
          transportPlan,
          checkInAt,
          expectedEndAt,
          codeWord,
          circleNote,
          shareLiveLocation,
          safeDateChecklist,
          circleCheckStatus,
          lastCircleCheckAt,
        ]
      properties:
        trustedCircleName:
          type: ["string", "null"]
          description: First name or label of the trusted person/circle. Phone numbers are not stored.
        transportPlan:
          type: ["string", "null"]
          description: User-entered transport or exit plan.
        checkInAt:
          type: ["string", "null"]
          format: date-time
        expectedEndAt:
          type: ["string", "null"]
          format: date-time
        codeWord:
          type: ["string", "null"]
          description: Optional word the user can share with trusted contacts.
        circleNote:
          type: ["string", "null"]
          description: Optional user-authored note shown in the Date Card preview.
        shareLiveLocation:
          type: boolean
          description: User intent only; the app does not continuously track by default.
        safeDateChecklist:
          $ref: "#/components/schemas/SafeDateChecklist"
        circleCheckStatus:
          oneOf:
            - $ref: "#/components/schemas/CircleCheckStatus"
            - type: "null"
        lastCircleCheckAt:
          type: ["string", "null"]
          format: date-time
        coverModeEnabled:
          type: boolean
        coverModeTheme:
          oneOf:
            - $ref: "#/components/schemas/CoverModeTheme"
            - type: "null"
        dateModeStatus:
          oneOf:
            - $ref: "#/components/schemas/DateModeStatus"
            - type: "null"
        dateModeStartedAt:
          type: ["string", "null"]
          format: date-time
        dateModeClosedAt:
          type: ["string", "null"]
          format: date-time
    SafeDateChecklist:
      type: object
      required:
        [
          publicPlace,
          ownTransport,
          circleHasPlan,
          profileReviewed,
          noPrivateLocationPressure,
          noMoneyOrPhotoPressure,
        ]
      properties:
        publicPlace: { type: boolean }
        ownTransport: { type: boolean }
        circleHasPlan: { type: boolean }
        profileReviewed: { type: boolean }
        noPrivateLocationPressure: { type: boolean }
        noMoneyOrPhotoPressure: { type: boolean }
    CircleCheckStatus:
      type: string
      enum: [planned, safe, needs_help, completed]
    DateModeStatus:
      type: string
      enum:
        [
          planning,
          date_card_sent,
          on_date,
          check_in_due,
          safe,
          needs_exit,
          home_safe,
          missed_check_in,
        ]
    CoverModeTheme:
      type: string
      enum: [clock, notes, breathing]
    DateSafetyPlanListStatus:
      type: object
      required:
        [
          hasPlan,
          hasTrustedCircle,
          hasTransportPlan,
          hasCheckIn,
          hasExpectedEnd,
          hasCodeWord,
          hasCircleNote,
          shareLiveLocation,
          safeDateChecklistReady,
          circleCheckStatus,
          lastCircleCheckAt,
          coverModeEnabled,
          coverModeTheme,
          dateModeStatus,
          dateModeStartedAt,
          dateModeClosedAt,
          updatedAt,
        ]
      properties:
        hasPlan: { type: boolean }
        hasTrustedCircle: { type: boolean }
        hasTransportPlan: { type: boolean }
        hasCheckIn: { type: boolean }
        hasExpectedEnd: { type: boolean }
        hasCodeWord: { type: boolean }
        hasCircleNote: { type: boolean }
        shareLiveLocation: { type: boolean }
        safeDateChecklistReady: { type: boolean }
        circleCheckStatus:
          oneOf:
            - $ref: "#/components/schemas/CircleCheckStatus"
            - type: "null"
        lastCircleCheckAt: { type: ["string", "null"], format: date-time }
        coverModeEnabled: { type: boolean }
        coverModeTheme:
          oneOf:
            - $ref: "#/components/schemas/CoverModeTheme"
            - type: "null"
        dateModeStatus:
          oneOf:
            - $ref: "#/components/schemas/DateModeStatus"
            - type: "null"
        dateModeStartedAt: { type: ["string", "null"], format: date-time }
        dateModeClosedAt: { type: ["string", "null"], format: date-time }
        updatedAt: { type: ["string", "null"], format: date-time }
    MatchStatus:
      type: string
      enum: [active, archived, ghosted]
    Match:
      type: object
      required:
        - id
        - name
        - photoObjectPath
        - status
        - vibeTags
        - extractedProfile
        - notes
        - nextDateAt
        - nextDateLocation
        - dateHistory
        - tags
        - createdAt
        - updatedAt
        - pendingScreenshotCount
        - failedScreenshotCount
        - analysisFreshness
        - lastDateBrief
        - dateBriefFreshness
        - dateSafetyPlanStatus
        - lastRead
        - readFreshness
        - redFlags
        - currentRedFlags
        - historicalRedFlags
        - redFlagSummary
      properties:
        id: { type: integer }
        name: { type: string }
        photoObjectPath: { type: ["string", "null"] }
        status:
          $ref: "#/components/schemas/MatchStatus"
        vibeTags:
          type: array
          items: { type: string }
        tags:
          type: array
          items: { type: string }
        extractedProfile:
          $ref: "#/components/schemas/ExtractedProfile"
        notes: { type: string }
        nextDateAt: { type: ["string", "null"], format: date-time }
        nextDateLocation: { type: ["string", "null"] }
        nextDateOutfit: { type: ["string", "null"] }
        dateHistory:
          type: array
          items:
            $ref: "#/components/schemas/DateHistoryEntry"
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        lastDateBrief:
          oneOf:
            - $ref: "#/components/schemas/DateBriefSnapshot"
            - type: "null"
        dateBriefFreshness:
          $ref: "#/components/schemas/DateBriefFreshness"
        dateSafetyPlanStatus:
          $ref: "#/components/schemas/DateSafetyPlanListStatus"
        lastRead:
          oneOf:
            - $ref: "#/components/schemas/MatchReadSnapshot"
            - type: "null"
        readFreshness:
          $ref: "#/components/schemas/MatchReadFreshness"
        redFlags:
          type: array
          items: { $ref: "#/components/schemas/RedFlag" }
        currentRedFlags:
          type: array
          items: { $ref: "#/components/schemas/RedFlag" }
        historicalRedFlags:
          type: array
          items: { $ref: "#/components/schemas/RedFlag" }
        redFlagSummary:
          $ref: "#/components/schemas/RedFlagSummary"
        scoreHistory:
          type: array
          items:
            $ref: "#/components/schemas/ScoreHistoryPoint"
        lastSpeaker:
          type: ["string", "null"]
          enum: [her, me, null]
          description: Speaker of the most recent transcript turn, if any
        lastActivityAt:
          type: ["string", "null"]
          format: date-time
          description: Timestamp of most recent screenshot upload (or null if none)
        pendingScreenshotCount:
          type: integer
          description: Screenshots awaiting analysis
        failedScreenshotCount:
          type: integer
          description: Screenshots whose analysis failed
        analysisFreshness:
          type: string
          enum: [current, needs-analysis, never-analyzed]
          description: Whether stored transcript reflects all screenshots
    Screenshot:
      type: object
      required:
        [
          id,
          matchId,
          objectPath,
          uploadedAt,
          extractionStatus,
          extractionError,
          rawImagePurgedAt,
        ]
      properties:
        id: { type: integer }
        matchId: { type: integer }
        objectPath: { type: ["string", "null"] }
        uploadedAt: { type: string, format: date-time }
        extractionStatus:
          type: string
          enum: [pending, done, failed]
        extractionError: { type: ["string", "null"] }
        rawImagePurgedAt: { type: ["string", "null"], format: date-time }
    TranscriptTurn:
      type: object
      required: [speaker, text]
      properties:
        speaker:
          type: string
          enum: [her, me]
        text: { type: string }
    MatchDetail:
      type: object
      required:
        - id
        - name
        - photoObjectPath
        - status
        - vibeTags
        - extractedProfile
        - notes
        - nextDateAt
        - nextDateLocation
        - dateHistory
        - transcript
        - tags
        - createdAt
        - updatedAt
        - screenshots
        - timelineEvents
        - pendingScreenshotCount
        - failedScreenshotCount
        - analysisFreshness
        - lastDateBrief
        - dateBriefFreshness
        - dateSafetyPlan
        - lastRead
        - readFreshness
        - redFlags
        - currentRedFlags
        - historicalRedFlags
        - redFlagSummary
      properties:
        id: { type: integer }
        name: { type: string }
        photoObjectPath: { type: ["string", "null"] }
        status:
          $ref: "#/components/schemas/MatchStatus"
        vibeTags:
          type: array
          items: { type: string }
        tags:
          type: array
          items: { type: string }
        extractedProfile:
          $ref: "#/components/schemas/ExtractedProfile"
        notes: { type: string }
        nextDateAt: { type: ["string", "null"], format: date-time }
        nextDateLocation: { type: ["string", "null"] }
        nextDateOutfit: { type: ["string", "null"] }
        dateHistory:
          type: array
          items:
            $ref: "#/components/schemas/DateHistoryEntry"
        lastDateBrief:
          oneOf:
            - $ref: "#/components/schemas/DateBriefSnapshot"
            - type: "null"
        dateBriefFreshness:
          $ref: "#/components/schemas/DateBriefFreshness"
        dateSafetyPlan:
          oneOf:
            - $ref: "#/components/schemas/DateSafetyPlan"
            - type: "null"
        lastRead:
          oneOf:
            - $ref: "#/components/schemas/MatchReadSnapshot"
            - type: "null"
        readFreshness:
          $ref: "#/components/schemas/MatchReadFreshness"
        redFlags:
          type: array
          items: { $ref: "#/components/schemas/RedFlag" }
        currentRedFlags:
          type: array
          items: { $ref: "#/components/schemas/RedFlag" }
        historicalRedFlags:
          type: array
          items: { $ref: "#/components/schemas/RedFlag" }
        redFlagSummary:
          $ref: "#/components/schemas/RedFlagSummary"
        transcript:
          type: array
          items:
            $ref: "#/components/schemas/TranscriptTurn"
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        screenshots:
          type: array
          items:
            $ref: "#/components/schemas/Screenshot"
        timelineEvents:
          type: array
          items:
            $ref: "#/components/schemas/MatchTimelineEvent"
        pendingScreenshotCount:
          type: integer
          description: Screenshots awaiting analysis
        failedScreenshotCount:
          type: integer
          description: Screenshots whose analysis failed
        analysisFreshness:
          type: string
          enum: [current, needs-analysis, never-analyzed]
          description: Whether stored transcript reflects all screenshots
    MatchCreateInput:
      type: object
      required: [screenshotObjectPath]
      properties:
        screenshotObjectPath:
          type: string
          description: Object path returned from /storage/uploads/request-url after upload
        name:
          type: string
          description: Optional initial name. If omitted, the server uses a placeholder until extraction completes.
    ExtractionPreview:
      type: object
      required: [suggestedName, vibeTags, extractedProfile]
      properties:
        suggestedName: { type: ["string", "null"] }
        vibeTags:
          type: array
          items: { type: string }
        extractedProfile:
          $ref: "#/components/schemas/ExtractedProfile"
    MatchUpdate:
      type: object
      properties:
        name: { type: string, minLength: 1 }
        notes: { type: string }
        status:
          $ref: "#/components/schemas/MatchStatus"
        vibeTags:
          type: array
          items: { type: string }
        tags:
          type: array
          items: { type: string }
        extractedProfile:
          $ref: "#/components/schemas/ExtractedProfile"
        photoObjectPath: { type: ["string", "null"] }
        nextDateAt: { type: ["string", "null"], format: date-time }
        nextDateLocation: { type: ["string", "null"] }
        nextDateOutfit: { type: ["string", "null"] }
        dateSafetyPlan:
          oneOf:
            - $ref: "#/components/schemas/DateSafetyPlanInput"
            - type: "null"
        dateHistory:
          type: array
          items:
            $ref: "#/components/schemas/DateHistoryEntry"
        transcript:
          type: array
          items:
            $ref: "#/components/schemas/TranscriptTurn"
    ScreenshotInput:
      type: object
      required: [objectPath]
      properties:
        objectPath: { type: string }
    VoiceDebriefInput:
      type: object
      required: [audioObjectPath]
      properties:
        audioObjectPath: { type: string }
        addToDateHistory:
          type: boolean
          description: If true, append a new date history entry summarizing this debrief.
    VoiceDebriefAnalysis:
      type: object
      required:
        - summary
        - vibe
        - greenFlags
        - redFlags
        - nextMoveSuggestion
        - tagsToAdd
        - date
        - readUpdate
        - timelineTitle
      properties:
        summary: { type: string }
        vibe: { type: ["string", "null"] }
        greenFlags:
          type: array
          items: { type: string }
        redFlags:
          type: array
          items: { type: string }
        nextMoveSuggestion: { type: ["string", "null"] }
        tagsToAdd:
          type: array
          items:
            type: object
            required: [tag, reason]
            properties:
              tag: { type: string }
              reason: { type: ["string", "null"] }
        date:
          type: object
          required: [isDate, when, location, recap]
          properties:
            isDate: { type: boolean }
            when: { type: ["string", "null"], format: date-time }
            location: { type: ["string", "null"] }
            recap: { type: ["string", "null"] }
        readUpdate: { type: ["string", "null"] }
        timelineTitle: { type: ["string", "null"] }
    StaleNudge:
      type: object
      required: [matchId, name, photoObjectPath, hoursSinceLastReply, openers]
      properties:
        matchId: { type: integer }
        name: { type: string }
        photoObjectPath: { type: ["string", "null"] }
        hoursSinceLastReply: { type: number }
        openers:
          type: array
          items: { type: string }
    VoiceNoteFeedbackResult:
      type: object
      required:
        - transcript
        - toneRating
        - energyRating
        - strengths
        - improvements
        - rewrite
        - shouldSend
      properties:
        transcript: { type: string }
        toneRating: { type: ["integer", "null"] }
        energyRating: { type: ["integer", "null"] }
        strengths:
          type: array
          items: { type: string }
        improvements:
          type: array
          items: { type: string }
        rewrite: { type: ["string", "null"] }
        shouldSend:
          type: string
          enum: [send, revise, scrap]
    InPersonRecordingInput:
      type: object
      required: [audioObjectPath, bothPartiesConsented]
      properties:
        audioObjectPath: { type: string }
        bothPartiesConsented:
          type: boolean
          description: Must be true. Server rejects the request otherwise.
        addToDateHistory: { type: boolean }
    VoiceDebriefResult:
      type: object
      required: [transcript, analysis, timelineEvents, match]
      properties:
        transcript: { type: string }
        analysis:
          $ref: "#/components/schemas/VoiceDebriefAnalysis"
        timelineEvents:
          type: array
          items:
            $ref: "#/components/schemas/MatchTimelineEvent"
        match:
          $ref: "#/components/schemas/MatchDetail"
    DateBriefResult:
      type: object
      required: [brief, generatedAt]
      properties:
        brief:
          { type: string, description: Markdown-formatted pre-date prep brief }
        generatedAt: { type: string, format: date-time }
    DateBriefSnapshot:
      type: object
      required: [brief, generatedAt, screenshotCountAt, contextHash]
      properties:
        brief: { type: string }
        generatedAt: { type: string, format: date-time }
        screenshotCountAt:
          type: integer
          description: Analyzed (extractionStatus=done) screenshot count at generation time
        contextHash:
          type: string
          description: Hash of date-related context (dateHistory, nextDate*, notes) at generation time
    DateBriefFreshness:
      type: string
      enum: [current, stale, missing]
      description: |
        current = brief is up to date.
        stale   = new analyzed screenshots, changed date details, or > 5 days old.
        missing = no brief has ever been generated.
    RedFlag:
      type: object
      required: [severity, label, evidence]
      properties:
        severity:
          type: string
          enum: [low, medium, high]
        label: { type: string }
        evidence: { type: string }
        status:
          type: string
          enum: [current, previously-seen]
        firstSeenAt: { type: string, format: date-time }
        lastSeenAt: { type: string, format: date-time }
        occurrenceCount: { type: integer }
    GreenFlag:
      type: object
      required: [label, evidence]
      properties:
        label: { type: string }
        evidence: { type: string }
    RedFlagRadarResult:
      type: object
      required:
        - redFlags
        - currentRedFlags
        - historicalRedFlags
        - greenFlags
        - overallRead
        - generatedAt
        - redFlagSummary
      properties:
        redFlags:
          type: array
          items: { $ref: "#/components/schemas/RedFlag" }
        currentRedFlags:
          type: array
          items: { $ref: "#/components/schemas/RedFlag" }
        historicalRedFlags:
          type: array
          items: { $ref: "#/components/schemas/RedFlag" }
        greenFlags:
          type: array
          items: { $ref: "#/components/schemas/GreenFlag" }
        overallRead: { type: string }
        generatedAt: { type: string, format: date-time }
        redFlagSummary:
          $ref: "#/components/schemas/RedFlagSummary"
    RedFlagSummary:
      type: object
      required:
        [currentCount, historicalCount, highSeverityCount, lastAnalyzedAt]
      properties:
        currentCount: { type: integer }
        historicalCount: { type: integer }
        highSeverityCount: { type: integer }
        lastAnalyzedAt: { type: ["string", "null"], format: date-time }
    MatchTimelineEvent:
      type: object
      required:
        - id
        - matchId
        - type
        - source
        - title
        - summary
        - body
        - metadata
        - occurredAt
        - createdAt
      properties:
        id: { type: integer }
        matchId: { type: integer }
        type:
          type: string
          enum:
            [
              date_scheduled,
              safety_plan_updated,
              voice_debrief,
              date_debrief,
              in_person_recording,
              manual_note,
              screenshot_import,
              chat_insight,
              red_flag_seen,
              green_flag_seen,
              tag_added,
              tag_removed,
            ]
        source:
          type: string
          enum: [user, ai, voice-debrief, in-person-recording, chat]
        title: { type: string }
        summary: { type: ["string", "null"] }
        body: { type: ["string", "null"] }
        metadata:
          type: object
          additionalProperties: true
        occurredAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
    CheatSheetReply:
      type: object
      required: [style, text]
      properties:
        style:
          type: string
          enum: [playful, curious, direct]
        text: { type: string }
    CheatSheetResult:
      type: object
      required: [replies]
      properties:
        replies:
          type: array
          items: { $ref: "#/components/schemas/CheatSheetReply" }
    ResponseStats:
      type: object
      required:
        - herAvgReplyHours
        - meAvgReplyHours
        - herMessageCount
        - meMessageCount
        - longestHerSilenceHours
        - cadenceBalance
      properties:
        herAvgReplyHours: { type: ["number", "null"] }
        meAvgReplyHours: { type: ["number", "null"] }
        herMessageCount: { type: integer }
        meMessageCount: { type: integer }
        longestHerSilenceHours: { type: ["number", "null"] }
        cadenceBalance:
          type: string
          enum: [you_chasing, balanced, she_chasing, unknown]
    WeeklyDebriefMatchSummary:
      type: object
      required: [matchId, name, status, reason]
      properties:
        matchId: { type: integer }
        name: { type: string }
        status:
          type: string
          enum: [heating_up, cold, needs_attention, deprioritize, steady]
        reason: { type: string }
    WeeklyDebriefResult:
      type: object
      required:
        [headline, summary, totalActive, newThisWeek, matches, recommendations]
      properties:
        headline: { type: string }
        summary: { type: string }
        totalActive: { type: integer }
        newThisWeek: { type: integer }
        matches:
          type: array
          items: { $ref: "#/components/schemas/WeeklyDebriefMatchSummary" }
        recommendations:
          type: array
          items: { type: string }
    AutoArchiveCandidate:
      type: object
      required: [matchId, name, photoObjectPath, hoursSinceLastReply, reason]
      properties:
        matchId: { type: integer }
        name: { type: string }
        photoObjectPath: { type: ["string", "null"] }
        hoursSinceLastReply: { type: number }
        reason: { type: string }
    TagSuggestion:
      type: object
      required: [tag, action, reason]
      properties:
        tag: { type: string }
        action:
          type: string
          enum: [add, remove]
        reason: { type: string }
    TagSuggestionResult:
      type: object
      required: [suggestions, summary]
      properties:
        suggestions:
          type: array
          items: { $ref: "#/components/schemas/TagSuggestion" }
        summary: { type: string }
    TagSuggestionApplyInput:
      type: object
      required: [suggestions]
      properties:
        suggestions:
          type: array
          items: { $ref: "#/components/schemas/TagSuggestion" }
    TagEvent:
      type: object
      required: [id, tag, action, source, createdAt]
      properties:
        id: { type: integer }
        tag: { type: string }
        action:
          type: string
          enum: [added, removed]
        source:
          type: string
          enum: [user, ai]
        reason: { type: ["string", "null"] }
        createdAt:
          type: string
          format: date-time
    TagHistoryResult:
      type: object
      required: [events]
      properties:
        events:
          type: array
          items: { $ref: "#/components/schemas/TagEvent" }
    FunnelStage:
      type: object
      required: [label, count]
      properties:
        label: { type: string }
        count: { type: integer }
    FunnelStats:
      type: object
      required: [stages, totals]
      properties:
        stages:
          type: array
          items: { $ref: "#/components/schemas/FunnelStage" }
        totals:
          type: object
          required:
            [
              matches,
              active,
              archived,
              ghosted,
              withDateScheduled,
              withDateCompleted,
            ]
          properties:
            matches: { type: integer }
            active: { type: integer }
            archived: { type: integer }
            ghosted: { type: integer }
            withDateScheduled: { type: integer }
            withDateCompleted: { type: integer }
    UploadUrlRequest:
      type: object
      required: [name, size, contentType]
      properties:
        name: { type: string, minLength: 1 }
        size: { type: integer, minimum: 1 }
        contentType: { type: string, minLength: 1 }
    UploadUrlResponse:
      type: object
      required: [uploadURL, objectPath]
      properties:
        uploadURL: { type: string, format: uri }
        objectPath: { type: string }
        metadata:
          $ref: "#/components/schemas/UploadUrlRequest"
    ChatConversation:
      type: object
      required: [id, title, matchId, createdAt]
      properties:
        id: { type: integer }
        title: { type: string }
        matchId: { type: ["integer", "null"] }
        createdAt: { type: string, format: date-time }
    ChatMessage:
      type: object
      required: [id, conversationId, role, content, createdAt]
      properties:
        id: { type: integer }
        conversationId: { type: integer }
        role: { type: string }
        content: { type: string }
        createdAt: { type: string, format: date-time }
    ChatConversationInput:
      type: object
      required: [title]
      properties:
        title: { type: string, minLength: 1 }
        matchId: { type: ["integer", "null"] }
    ChatMessageInput:
      type: object
      required: [content]
      properties:
        content: { type: string, minLength: 1 }
    ChatConversationWithMessages:
      type: object
      required: [id, title, matchId, createdAt, messages]
      properties:
        id: { type: integer }
        title: { type: string }
        matchId: { type: ["integer", "null"] }
        createdAt: { type: string, format: date-time }
        messages:
          type: array
          items:
            $ref: "#/components/schemas/ChatMessage"
